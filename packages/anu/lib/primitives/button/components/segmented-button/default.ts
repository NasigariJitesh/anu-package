import { SegmentedButtonProps } from './../../types/segmented-button';

/**
 * Default Properties of the segmented button component
 */
export const defaultProps: Omit<SegmentedButtonProps, 'id' | 'title'> = {};
