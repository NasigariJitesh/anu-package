import { SliderProps } from './../types/slider';

/**
 * the default props for the slider component
 */
export const defaultProps: SliderProps = {
  value: 0,
  minimumValue: 0,
  maximumValue: 1,
};
