export { useAccordionContext } from './accordion';

import { default as Container } from './accordion';
import { default as Children } from './accordion-children';
import { default as Header } from './accordion-header';

const Accordion = {
  Children,
  Header,
  Container,
};

export default Accordion;
