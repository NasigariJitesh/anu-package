import Content from 'components/content';
import { timePickerDocumentation } from 'services/docs/time-picker';

const TimePicker = () => {
  return <Content values={timePickerDocumentation} />;
};

export default TimePicker;
