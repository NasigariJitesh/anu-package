import Content from 'components/content';
import { datePickerModalDocumentation } from 'services/docs/date-picker-modal';

const DatePickerModal = () => {
  return <Content values={datePickerModalDocumentation} />;
};

export default DatePickerModal;
