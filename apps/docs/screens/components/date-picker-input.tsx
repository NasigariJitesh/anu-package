import Content from 'components/content';
import { datePickerInputDocumentation } from 'services/docs/date-picker-input';

const DatePickerInput = () => {
  return <Content values={datePickerInputDocumentation} />;
};

export default DatePickerInput;
