import Content from 'components/content';
import { dialogTitleDocumentation } from 'services/docs/dialog-title';

const DialogTitle = () => {
  return <Content values={dialogTitleDocumentation} />;
};

export default DialogTitle;
