import Content from 'components/content';
import { sliderDocumentation } from 'services/docs/slider';

const Slider = () => {
  return <Content values={sliderDocumentation} />;
};

export default Slider;
