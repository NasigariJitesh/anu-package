import Content from 'components/content';
import { dividerDocumentation } from 'services/docs/divider';

const Divider = () => {
  return <Content values={dividerDocumentation} />;
};

export default Divider;
