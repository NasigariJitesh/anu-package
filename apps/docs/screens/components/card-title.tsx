import Content from 'components/content';
import { cardTitleDocumentation } from 'services/docs/card-title';

const CardTitle = () => {
  return <Content values={cardTitleDocumentation} />;
};

export default CardTitle;
